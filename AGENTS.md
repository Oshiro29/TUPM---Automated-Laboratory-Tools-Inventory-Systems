# AGENTS.md — Agent guidance for this workspace

Purpose
- Help AI coding agents quickly become productive in this frontend repo (Vite + React).

Quick-start (commands)
- Install deps: `npm install`
- Start dev server: `npm run dev`
- Build production: `npm run build`
- Preview build: `npm run preview`
- Lint sources: `npm run lint`

Key files & entrypoints
- `package.json` — npm scripts and dependencies. See [package.json](package.json).
- `vite.config.js` — Vite configuration. See [vite.config.js](vite.config.js).
- `index.html` — Application HTML shell. See [index.html](index.html).
- `src/main.jsx` — App bootstrap and React root. See [src/main.jsx](src/main.jsx).
- `src/App.jsx` — Top-level app component. See [src/App.jsx](src/App.jsx).
- `src/components/` — UI components and kiosks. Useful files:
  - [src/components/KioskLandingPage.jsx](src/components/KioskLandingPage.jsx)
  - [src/components/KioskToolSelection.jsx](src/components/KioskToolSelection.jsx)
  - [src/components/KioskSelectTransaction.jsx](src/components/KioskSelectTransaction.jsx)
  - [src/components/KioskReturnVerification.jsx](src/components/KioskReturnVerification.jsx)
  - [src/components/KioskReturnConfirmation.jsx](src/components/KioskReturnConfirmation.jsx)
  - [src/components/KioskCoinDepositVerification.jsx](src/components/KioskCoinDepositVerification.jsx)
  - [src/components/KioskToolReleaseConfirmation.jsx](src/components/KioskToolReleaseConfirmation.jsx)
  - [src/components/AdminLiveMonitoringDashboard.jsx](src/components/AdminLiveMonitoringDashboard.jsx)

Guidance for agents
- Follow the "link, don't embed" principle: reference existing docs rather than copying them.
- Avoid global refactors without tests; this repo is a small frontend app — prefer focused changes.
- Use `npm run lint` before committing code changes.
- If adding runtime behavior, run the dev server and verify UI flows manually.

What I couldn't discover automatically
- No test framework configured (no `test` script). Add tests before large refactors.
- No CONTRIBUTING.md or architecture doc found; consider adding `client_docs/` notes into a short `CONTRIBUTING.md`.

Suggested next agent customizations
- Create `.github/copilot-instructions.md` summarizing quick commands and PR guidelines.
- Add a small skill or prompt template for "Add UI story + component test" that runs the dev server and scaffolds testing dependencies.

---
Generated by an AI assistant to accelerate agent onboarding.
