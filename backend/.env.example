# Optional. Defaults to backend/database/inventory.sqlite.
DATABASE_PATH=./database/inventory.sqlite

# Change this before deploying outside development.
ADMIN_ACCESS_TOKEN=ADMIN-TUPM-ACCESS

PORT=5000
